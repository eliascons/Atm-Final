{
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd Atm-Final/bank-client && npm install --only=dev && npm install && npm run build"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.4.2"
  },
  "type": "module"
}
